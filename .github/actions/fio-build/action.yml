name: "Build FIO with hipFile support"

runs:
  using: "composite"
  steps:
    - name: "Step 1"
      run: |
        echo "Running step 1"
    - name: "Step 2"
      run: |
        echo "Running step 2"
